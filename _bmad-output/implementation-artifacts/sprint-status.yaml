# Sprint Status — Sherab
#
# NOTE: built by hand, not by scripts/sprint_plan.py. Two blockers still make
# the deterministic path inapplicable, even with `uv` now installed:
#   1. This project has no markdown epic file (`## Epic N:` / `### Story
#      N.M:` headings) for the script's regex parser to read — it uses the
#      lightweight bmad-spec track instead, whose story breakdown lives in
#      `_bmad-output/specs/spec-class-tracker/stories.yaml` (a YAML file,
#      plus its own `spec_checkpoint`/`done_checkpoint` gate flags unrelated
#      to this file's status vocabulary) — a format the script's regex
#      parser was never built to read, regardless of `uv` availability.
#   2. That YAML file's story ids/format don't match the script's expected
#      key derivation either.
# REFRESHED 2026-09-15 18:55: Story 3-1 (one-off homework assignment &
# review) went through bmad-build (implement → review/patch cycle, 9
# findings fixed -- including a real RLS gap letting a teacher write
# status for a class they don't teach, caught before merge -- 1 deferred,
# 4 rejected as false/by-design -> full check/eslint/test suite green)
# and a bmad-walkthrough human review (Approve). Its spec is `status:
# done`; sprint-planning mirrors that here.
# REFRESHED 2026-09-16: Story 3-2 (recurring homework assignments) went
# through bmad-build (implement → review/patch cycle, 10 findings fixed --
# including an unrestricted homework_assignments UPDATE policy letting a
# teacher convert a one-off assignment into a recurring series after
# creation, an unbounded due_offset_days CHECK letting one bad assignment
# abort the shared nightly generator run for every recurring assignment,
# and an unbounded catch-up loop with no iteration cap -- 2 deferred
# (bo.json Tibetan translations, action-layer test coverage), 2 rejected
# as false/by-design -> full check/eslint/test suite green, 104 tests
# passing) and a bmad-walkthrough human review (Approve). Its spec is
# `status: done`; sprint-planning mirrors that here. It was epic-3's
# last story, so epic-3 is now `done`.
# All other stories: no spec file exists yet, so `backlog`.
# Rerun `bmad-sprint-planning` to refresh after stories.yaml or story specs change.

generated: 09-13-2026 17:46
last_updated: 09-16-2026 00:00
project: Sherab
project_key: NOKEY
tracking_system: file-system
story_location: "_bmad-output/specs/spec-class-tracker/stories"

development_status:
  epic-1: done # Accounts & Roles (CAP-1)
  1-1-teacher-student-account-management: done
  1-2-student-self-registration-approval: done
  epic-1-retrospective: optional

  epic-2: done # Roster & Skill Tracking (CAP-2)
  2-1-roster-skill-status-tracking: done
  epic-2-retrospective: optional

  epic-3: done # Homework (CAP-3)
  3-1-one-off-homework-assignment-review: done
  3-2-recurring-homework-assignments: done
  epic-3-retrospective: optional

  epic-4: backlog # Gamification (CAP-4, CAP-5, CAP-6)
  4-1-streaks: backlog
  4-2-badges: backlog
  4-3-team-leaderboard: backlog
  epic-4-retrospective: optional

  epic-5: backlog # Admin Oversight & Privacy (CAP-7, CAP-8)
  5-1-admin-cross-class-oversight-data-deletion: backlog
  epic-5-retrospective: optional

action_items: []
