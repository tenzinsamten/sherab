---
topic: Munich Tibetan Sunday School Class Tracker app
source: class-tracker-requirements.md v1.3
updated: 2026-09-25T20:03
---

- (capability) CAP-1 Account & registration — intent: admin creates/verifies teacher accounts and assigns them to classes (many-to-many); students self-register with name+class code, give guardian consent, and land in Pending until a class teacher or admin approves. success: an unapproved student is invisible on any roster/leaderboard and cannot log progress; an approved student appears everywhere and can act.
- (capability) CAP-2 Roster & skill tracking — intent: any teacher assigned to a class can view/edit that class's roster, mark weekly attendance, and set a per-student per-skill-area (language/song/dance) status of Not started/Learning/Confident, with full history retained and optional free-text notes. success: a substitute teacher opens a class they're assigned to and sees complete status history, not just the latest value; a teacher not assigned to a class cannot see it.
- (capability) CAP-3 Homework assignment & tracking — intent: a teacher creates one-off or recurring homework (title, skill area, target students, due date, optional reference links) scoped to their own class; a student sees open homework from their class, opens reference links externally, and self-marks Done; a teacher reviews to Reviewed. success: a recurring assignment auto-generates each period with independent per-instance Done/Reviewed status; overdue+not-Done items stay visible flagged overdue until explicitly archived, never silently expiring.
- (capability) CAP-4 Streaks — intent: the system tracks a per-student streak of consecutive weeks with both attendance and homework Done (Reviewed not required), surviving an admin-configurable number of missed weeks (default 2). success: a student who attends and marks homework Done in week N keeps their streak; missing more than the configured grace window resets it, and changing the grace value requires no code change.
- (capability) CAP-5 Badges — intent: a student earns badges at configurable milestone increments of attendance count and homework-done count, visible only on their own profile. success: crossing a milestone (e.g. 5th class attended) awards the corresponding badge without ranking the student against peers.
- (capability) CAP-6 Team leaderboard — intent: students belong to a fixed-for-the-year team (manually assigned by their teacher at approval/onboarding time, including mid-year joins), and teams are ranked by a combined-streak metric, visible only within the school. success: the leaderboard reflects team standings by combined streak and never triggers automatic reassignment or public exposure.
- (capability) CAP-7 Admin oversight — intent: the admin manages teacher accounts and class assignments, acts as backup approver for any class's pending student registrations, holds the only cross-class view (all teachers, students, overall homework completion), and reviews/actions data deletion requests. success: the admin can approve/reject a registration for any class and see aggregate completion across all classes; no other role has cross-class visibility.
- (capability) CAP-8 Data privacy & deletion — intent: a student can submit a data deletion request from within the app; the admin reviews and must explicitly approve before any of that student's profile, progress, or homework records are erased. success: no deletion occurs without a recorded admin approval step, and once approved all of that student's records are gone.
- (constraint) Teachers are created and verified admin-side only — no teacher self-registration.
- (constraint) Student registration requires guardian consent capture and Pending-state gating before any visibility or activity — guards against dummy/duplicate sign-ups.
- (constraint) Class-scoped visibility, not teacher-owned: all teachers assigned to a class share full view/edit of that class's roster and homework; a teacher sees nothing for classes they're not assigned to.
- (constraint) Skill-status changes must retain full history, not just current value, so a substitute teacher has continuity.
- (constraint) Homework Done (self-report) alone is sufficient to count toward a streak; Reviewed is a separate teacher-confirmed state tracked for skill-mastery history and does not gate the streak.
- (constraint) Overdue, not-Done assignments never auto-expire — they stay visible flagged overdue until a teacher explicitly archives them.
- (constraint) Streak grace period (default 2 missed weeks) must be an admin-configurable value, not hardcoded.
- (constraint) Homework look-ahead window (default: current + next week) must be configurable in the data model without a structural change.
- (constraint) Leaderboard teams are fixed for the school year and manually assigned by teachers — no auto-balancing or randomization, including for mid-year joins.
- (constraint) Minimize personal data collected on minors (nickname + progress only); no public-facing leaderboard or profile.
- (constraint) Data deletion is never automatic — requires an explicit, recorded admin approval step before any erasure.
- (constraint) UI must support German, English, and Tibetan.
- (constraint) Must run as a low/near-free-cost PWA usable on existing family phones/tablets, no app-store install required.
- (constraint) Must tolerate two teachers editing the same class concurrently without clobbering each other's updates.
- (non-goal) Parent-facing login/view is deferred to a future phase — v1 has no separate parent account.
- (non-goal) A structured Duolingo-style curriculum engine (lesson sequencing, spaced repetition, adaptive difficulty, speech recognition) is out of scope for v1; only plain outbound reference links are in scope.
- (non-goal) No embedded in-app media player/preview for reference links in v1 — links open externally as plain 'Open reference' actions.
- (non-goal) Photo/audio proof-of-completion attachments are a possible future option, not v1 — homework completion is self-reported only.
- (non-goal) Offline queueing/sync is a nice-to-have, not day-one scope.
- (non-goal) A public/marketing website or video-lesson platform for the school is out of scope.
- (decision) Success signal: v1 ships directly to the school's own teachers (no separate requirements-gathering round) and is validated by one real Sunday-session end-to-end run — admin onboards a teacher, a teacher approves a pending student, marks attendance, assigns one-off and recurring homework with a reference link, the student marks it done, the teacher reviews it, and streak/badge/team-leaderboard reflect it correctly — then iterates from real teacher feedback.
- (assumption) Assumed a single admin role/account model for v1 — source describes one admin view with no multi-admin delegation or admin-of-admins concerns.
- (question) Badge milestone step size (every 5 vs every 10 attendances/homework-done) is explicitly left TBD in the source — needs confirmation once building.
- (question) GDPR-specific obligations beyond the described consent-at-registration and admin-approved-deletion flow (data controller identity, retention limits, breach notification) are unaddressed, and this app stores EU minors' personal data — needs a compliance decision before storing real student data.
- (note) Routed detailed homework lifecycle (creation fields, recurring mechanics, review state machine, overdue rules) to companion homework-workflow.md — too detailed for kernel Capabilities.
- (note) Routed stakeholder table and full registration/approval/visibility rules to companion roles-and-permissions.md.
- (note) Routed streak/badge/leaderboard numeric rules and team-assignment mechanics to companion gamification.md.
- (constraint) Munich Tibetan group owns the app and its data long-term, not any individual — bears on hosting-account ownership, domain, and admin-access continuity.
- (constraint) Device access is confirmed reliable (phone/tablet per family) — no paper/offline fallback path is needed for homework or streak tracking.
- (constraint) Must be simple enough for non-technical volunteer teachers to use without training.
- (non-goal) Replacing in-class teaching itself is out of scope — the app supports continuity/motivation around classes, not a substitute.
- (event) Self-validate pass 1 (coherence, rules 1-6+8): PASS — all 8 capabilities have intent+success, constraints all bend design, 7 non-goals present, success signal is a concrete demonstrable scenario, capability IDs stable/unique, prose lean.
- (event) Self-validate pass 2 (preservation): initial render missed 3 load-bearing NFR claims (data ownership, confirmed device access, non-technical-teacher simplicity) and 1 explicit non-goal (replacing in-class teaching) — all 4 added to Constraints/Non-goals. Re-check: all source claims now accounted for in SPEC.md or a companion. PASS after fix.
- (note) User requested Story Breakdown; also flagged tech stack/architecture is not yet decided — stories must stay WHAT-level, no stack assumptions in dispatch notes.
- (decision) Story 1 (CAP-1): Teacher & student account management — admin creates/verifies teachers and assigns classes; students self-register, give consent, land Pending, get approved by class teacher or admin backup. Foundational — everything else depends on roles existing. spec_checkpoint=true, done_checkpoint=true (also the point where the architecture/stack choice, still undecided, first gets made).
- (decision) Story 2 (CAP-2): Roster & skill-status tracking — attendance marking, per-skill-area status with history, free-text notes, class-scoped visibility. Depends on Story 1. spec_checkpoint=false, done_checkpoint=true.
- (decision) Story 3 (CAP-3 core): One-off homework assignment & review — create/assign, student self-marks Done, teacher marks Reviewed, overdue flagging/archiving. Depends on Story 2. spec_checkpoint=true (review-state machine is load-bearing), done_checkpoint=true.
- (decision) Story 4 (CAP-3 extension): Recurring homework assignments — series setup, auto-generated independent per-instance status, edit/end semantics. Depends on Story 3. spec_checkpoint=false, done_checkpoint=false.
- (decision) Story 5 (CAP-4): Streaks — consecutive-week tracking off attendance+homework-Done, admin-configurable grace period. Depends on Stories 2 and 3. spec_checkpoint=true (the grace-period configurability constraint must be designed in, not bolted on), done_checkpoint=false.
- (decision) Story 6 (CAP-5): Badges — configurable milestone increments on attendance/homework counts, profile-only visibility. Depends on Stories 2 and 3, independent of streaks. spec_checkpoint=false, done_checkpoint=false.
- (decision) Story 7 (CAP-6): Team leaderboard — fixed-for-year teams, manual assignment at approval time, ranked by combined streak. Depends on Story 5 and touches Story 1's approval flow for team assignment. spec_checkpoint=true (team-assignment touchpoint into Story 1 needs a deliberate seam), done_checkpoint=true.
- (decision) Story 8 (CAP-7, CAP-8): Admin cross-class oversight & data deletion — aggregate cross-class dashboard, admin backup-approve (mechanism shared with Story 1), deletion-request submission and admin-approved erasure. Depends on Story 1. spec_checkpoint=false, done_checkpoint=true.
- (note) User also flagged UI/UX design is not decided yet (no DESIGN.md/EXPERIENCE.md) — same caveat as the tech stack: student-facing, motivation-heavy stories (homework, streaks, badges, leaderboard) should not assume a visual design; dev dispatch notes flag this rather than inventing UI.
- (event) stories.yaml validity check: 8 entries, ids "1"-"8" unique and prefix-free, all required fields present, no status field, ids quoted strings. PASS. Tech-stack and UI/UX-design gaps both flagged in invoke_dev_with rather than assumed.
- (decision) Restructured stories.yaml from a flat 1-8 id list into 5 features using composite ids (epic sits inside this larger project spec, per stories-schema.md): Feature 1 Accounts & Roles (CAP-1) -> story 1-1; Feature 2 Roster & Skill Tracking (CAP-2) -> story 2-1; Feature 3 Homework (CAP-3) -> stories 3-1, 3-2; Feature 4 Gamification (CAP-4/5/6) -> stories 4-1, 4-2, 4-3; Feature 5 Admin Oversight & Privacy (CAP-7/8) -> story 5-1. Same 8 stories, same content/dependencies/checkpoints as the prior flat decomposition, user-approved grouping.
- (event) stories.yaml re-derive validity check: 8 entries, composite ids 1-1, 2-1, 3-1, 3-2, 4-1, 4-2, 4-3, 5-1 unique and prefix-free, all required fields present, no status field, ids quoted strings. PASS. In-progress draft spec file stories/1-teacher-student-account-management.md (unapproved, status draft) renamed to stories/1-1-teacher-student-account-management.md to match the new id; no content changes needed, no stale self-references found.
- (direction) 2026-09-25 user asked to add a calendar feature: admin plans school-wide class days (several classes can run on one day); teacher decides which days and what time their class meets; student tells class/team whether they are coming or on leave; class start time and duration can differ per day.
- (decision) Admin class days bound teachers: a class session can only exist on a day the admin marked as a class day; teachers set start time and duration per day within those days (user choice 2026-09-25).
- (decision) Each class has a default start time and duration; a session is created for every admin class day with those defaults, and any teacher assigned to the class can change one day's start time/duration or cancel that day without affecting other days (user choice 2026-09-25).
- (decision) Student marks Coming or On leave per upcoming session; visible to the class's teachers, classmates and the student's team. Announced leave does not consume streak grace, same as a holiday (user choice 2026-09-25).
- (decision) Scheduled sessions become the source of truth: attendance is marked against a scheduled session, and a week with no non-cancelled session for a class is a holiday for streaks — replaces inferring holidays from missing attendance rows (user choice 2026-09-25).
- (capability) CAP-9 School calendar — intent: admin marks the school-wide days on which classes happen; several classes can run on the same day. success: no class session can be scheduled on a day the admin has not marked; every role sees the same class days.
- (capability) CAP-10 Class sessions — intent: each class has a default start time and duration; every admin class day gets a session for the class with those defaults, and any teacher assigned to the class can change a single day's start time/duration or cancel it. success: changing or cancelling one day leaves other days untouched, and students of the class see that day's actual start time and duration.
- (capability) CAP-11 Attendance intent — intent: a student marks each upcoming session of a class they are enrolled in as Coming or On leave, visible to that class's teachers, classmates and the student's team. success: before class a teacher sees who is coming and who is on leave; an announced leave does not consume streak grace.
- (decision) CAP-2 amended: attendance is marked per scheduled session (CAP-10), not per free-chosen weekly date. Supersedes 'mark weekly attendance'.
- (constraint) Class sessions only on admin class days; per-day start time and duration are overrides on the class default, never a change to the default itself.
- (constraint) Streak holidays are defined by the calendar: a week with no non-cancelled session for the class excludes that week from grace; an announced leave also excludes the student's missed session from grace.
- (assumption) Session times are Munich local time (Europe/Berlin); no multi-timezone support.
- (assumption) Leave protects the streak only when set before the session starts, so leave cannot retroactively excuse an absence.
- (assumption) Streak stays weekly: if a class meets on more than one day in a week, attending any session of that week qualifies.
- (assumption) A student who has not answered is shown as not answered and treated as expected; no reason is collected for leave (minimize data on minors).
- (assumption) Admin removing a class day cancels every class session on that day.
- (question) Should students be notified (push/email) when a session is moved or cancelled, or is in-app display enough for v1?
- (question) Can a student set leave for a date range (e.g. a 3-week holiday) or only per session?
- (question) Should the app warn when a student enrolled in two classes has overlapping sessions on the same day?
- (question) Is calendar export/sync (iCal, Google Calendar) wanted, or in-app calendar only?
- (question) Leave is visible to the student's team across classes — confirm this is acceptable under the minimize-data-on-minors constraint (it shows nickname + Coming/On leave only).
- (note) Routed calendar/session rules, attendance-intent states and streak interaction to new companion calendar.md; streak holiday/leave rule also reflected in gamification.md.
- (event) Calendar update self-validate pass 1 (coherence, rules 1-6+8): PASS — CAP-9/10/11 each have intent+success, intents are WHAT-level (no stack), 2 new constraints each rule out designs (sessions off class days; inferred holidays), IDs continue from CAP-8 with none reused, CAP-2 amended in place keeping its ID.
- (event) Calendar update self-validate pass 2 (preservation): PASS — all 4 user points landed: admin class days + multiple classes per day (CAP-9), teacher chooses days/time (CAP-10 within CAP-9 days), student coming/leave to class+team (CAP-11), per-day start time and duration (CAP-10, calendar.md). Unconfirmed calls logged as 5 assumptions; 5 gaps as open questions.
- (event) stories.yaml drift: story 2-1 description ('mark weekly attendance') and 4-1 (holidays inferred) no longer match amended CAP-2/CAP-4 constraints; existing stories are done so they are left as-is and the change lands in new calendar stories.
- (decision) Story 6-1 (CAP-9, CAP-10): Class days & sessions — admin class-day calendar, class default start time/duration, per-day override/cancel by any class teacher, calendar visible to every role. spec_checkpoint=true, done_checkpoint=true (user 2026-09-25).
- (decision) Story 6-2 (CAP-2 amendment, CAP-4): Attendance per session & calendar holidays — attendance marked against a scheduled session, existing session_date marks backfilled, streak holiday = week with no non-cancelled session. Depends on 6-1. spec_checkpoint=true, done_checkpoint=true (user 2026-09-25).
- (decision) Story 6-3 (CAP-11): Student Coming / On leave — per-session attendance intent, visible to class teachers, classmates and team; leave set before start protects streak. Depends on 6-1 and 6-2. spec_checkpoint=true, done_checkpoint=true (user 2026-09-25).
- (event) stories.yaml validity check after calendar stories: 11 entries, ids unique/prefix-free/quoted, required fields present, no status field. PASS.
- (note) Correction to previous event: stories.yaml has 12 entries (1-1,1-2,2-1,3-1,3-2,4-1,4-2,4-3,5-1,6-1,6-2,6-3), not 11; verdict PASS unchanged.
